import { CanActivateFn } from '@angular/router';

export const routeGuard: CanActivateFn = (route, state) => {
  // Placeholder guard - sẵn sàng tích hợp phân quyền/xác thực sau này
  return true;
};
